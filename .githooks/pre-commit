#!/bin/sh

branch="$(git symbolic-ref --quiet --short HEAD 2>/dev/null || true)"

if [ "$branch" = "main" ]; then
  cat >&2 <<EOF
Direct commits to 'main' are blocked.

Create a short-lived branch from main and open a pull request.
EOF
  exit 1
fi

exit 0
